CREATE TABLE  "STUDENT" 
   (	"EMAIL" VARCHAR2(255), 
	"NAAM" VARCHAR2(255) NOT NULL ENABLE, 
	"TELEFONNNUMMER" VARCHAR2(255), 
	 CONSTRAINT "STUDENT_PK" PRIMARY KEY ("EMAIL") ENABLE
   ) ;

CREATE TABLE  "OPLEIDING" 
   (	"OPLEIDINGSNAAM" NUMBER, 
	"KERNDOCENT" VARCHAR2(255), 
	 CONSTRAINT "OPLEIDING_PK" PRIMARY KEY ("OPLEIDINGSNAAM") ENABLE
   ) ;

CREATE TABLE  "VOORLICHTINGSMOMENT" 
   (	"RONDE" NUMBER, 
	"OMSCHRIJVING" VARCHAR2(255), 
	 CONSTRAINT "VOORLICHTINGSMOMENT_PK" PRIMARY KEY ("RONDE") ENABLE
   ) ;

CREATE TABLE  "INSCHRIJVING" 
   (	"STUDENTEMAIL" VARCHAR2(255), 
	"VOORLICHTINGSMOMENTRONDE" NUMBER, 
	"OPLEIDINGOPLEIDINGSNAAM" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "INSCHRIJVING_PK" PRIMARY KEY ("STUDENTEMAIL", "VOORLICHTINGSMOMENTRONDE") ENABLE
   ) ;ALTER TABLE  "INSCHRIJVING" ADD CONSTRAINT "INSCHRIJVING_FK" FOREIGN KEY ("STUDENTEMAIL")
	  REFERENCES  "STUDENT" ("EMAIL") ENABLE;ALTER TABLE  "INSCHRIJVING" ADD CONSTRAINT "INSCHRIJVING_FK2" FOREIGN KEY ("VOORLICHTINGSMOMENTRONDE")
	  REFERENCES  "VOORLICHTINGSMOMENT" ("RONDE") ENABLE;ALTER TABLE  "INSCHRIJVING" ADD CONSTRAINT "INSCHRIJVING_FK3" FOREIGN KEY ("OPLEIDINGOPLEIDINGSNAAM")
	  REFERENCES  "OPLEIDING" ("OPLEIDINGSNAAM") ENABLE;

workspace: LUC4
Udername: LUC4

pasword:3Poten32